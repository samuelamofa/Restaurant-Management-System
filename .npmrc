# NPM Configuration for Monorepo
# Ensures workspace dependencies are installed correctly
legacy-peer-deps=false
save-exact=false
package-lock=true

# For Railway deployment - ensure workspaces work correctly
workspaces-update=false

